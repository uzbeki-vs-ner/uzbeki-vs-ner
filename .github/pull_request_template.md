## Summary

<!-- Why this change exists, not just what you edited. -->

## Checklist

- [ ] No datasets, zips, jsonl dumps, checkpoints, pretrained weights, or MLflow db/runs
- [ ] No Hugging Face / DVC cache blobs
- [ ] Commit messages have a subject, a blank line, and a real body (why, not just "wip"/"fix")
- [ ] CI-relevant changes still pass `make lint` and `make test` without downloading models
